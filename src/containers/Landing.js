import React, { Fragment } from 'react';
import NavBar from '../components/NavBar';

export const Landing = () => {
    return (
        <Fragment>
            <NavBar />
        </Fragment>
    );
};
